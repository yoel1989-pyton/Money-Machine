# ============================================================
# ANOINTMENT PROTOCOL - Railway Build Configuration
# ============================================================
# This enables FFmpeg for video fingerprint obfuscation
# ============================================================

[phases.setup]
nixPkgs = ['ffmpeg', 'ffmpeg-full', 'python311', 'openssl', 'wget', 'bash']

[phases.install]
cmds = ['pip install --upgrade pip', 'pip install -r requirements.txt']

[phases.build]
cmds = ['ffmpeg -version || echo "FFmpeg check"']

[start]
cmd = 'uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}'
